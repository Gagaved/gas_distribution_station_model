name: gas_distribution_station_model
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: null
  fluent_ui: ^4.5.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  json_theme: ^4.0.0
  flutter_side_menu: ^0.3.2
  floor: any
  faker: any
  tuple: ^2.0.1
  file_picker: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any
  flutter_lints: ^2.0.0
  msix: ^3.14.2

msix_config:
  identity_name: abc
  display_name: GdsPipeline
  publisher_display_name: Morozov
  languages: en-us
flutter:
  assets:
    - assets/appainter_theme.json
    - assets/valve_image.png
    - assets/percentage_valve_image.png
    - assets/meter_image.png
    - assets/heater_image.png
    - assets/reducer_image.png
    - assets/filter_image.png
    - assets/adorizer_image.png
    - assets/GRS.png
  uses-material-design: true